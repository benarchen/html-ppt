# 主题模板

1. 复制本目录并将目录名改为主题名称。
2. 将合法参考图放入 `references/`，记录来源、授权和 SHA-256。
3. 完成 `style-spec.json`，区分观察事实、推断、置信度和待确认项。
4. 更新 `theme.json`、`tokens.css` 和 `components.css`。
5. 用 `specimen.md` 覆盖全部 12 种标准布局。
6. 执行主题契约、严格 Preflight 和视觉回归。
7. 审核参考图、Style Spec、contact sheet 和视觉基线后锁定版本。

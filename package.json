{
  "name": "html-ppt",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Markdown-driven, HTML-first presentation generator",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=24 <27"
  },
  "bin": {
    "html-ppt": "./build/src/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "browser:install": "node scripts/run-playwright.mjs install chromium",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node scripts/run-node-tests.mjs",
    "test:browser": "npm run build && node scripts/run-browser-tests.mjs",
    "test:visual": "npm run build && node scripts/run-playwright.mjs test",
    "test:visual:update": "npm run build && node scripts/run-playwright.mjs test --update-snapshots",
    "html-ppt": "node build/src/cli.js",
    "deck:build": "npm run build --silent && node build/src/cli.js build",
    "deck:preview": "npm run build --silent && node build/src/cli.js preview",
    "deck:export": "npm run build --silent && node build/src/cli.js export",
    "deck:check": "npm run build --silent && node build/src/cli.js check",
    "check:themes": "npm run build --silent && node build/src/cli.js check-themes"
  },
  "dependencies": {
    "jszip": "3.10.1",
    "remark-directive": "4.0.0",
    "remark-frontmatter": "5.0.0",
    "remark-gfm": "4.0.1",
    "remark-parse": "11.0.0",
    "unified": "11.0.5",
    "yaml": "2.9.0"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "@types/node": "24.13.3",
    "pdfjs-dist": "6.3.289",
    "typescript": "7.0.2"
  }
}
